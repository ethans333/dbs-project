/*
    
    SuperAdmin Permissions: 
    1. SuperAdmin can view all events with their organization.
    2. SuperAdmin can view all comments on events.
    3. SuperAdmin can add comments to events.
    4. SuperAdmin can add new events.
    5. SuperAdmin can create new organizations.
*/

import LeftSideMenu from "../../Components/LeftSideMenu";

export default function () {
  return (
    <div>
      <LeftSideMenu />
      {/* Top Header that includes "[organization name] Events" */}
      {/* List of Events */}
      {/**/}
    </div>
  );
}
